{
  "name": "jejakmufassir-email-api",
  "version": "1.0.0",
  "description": "API untuk mengirim email notifikasi order",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "nodemailer": "^6.9.8"
  },
  "engines": {
    "node": ">=16.x"
  }
}
